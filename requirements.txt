pygame>=2.5.0
numpy>=1.24.0
pymunk>=6.0.0

# ML / Training
torch>=2.1.0
torchvision>=0.16.0
tqdm>=4.60.0

# Perceptual loss
lpips>=0.1.4

# Logging
wandb>=0.16.0
weave>=0.1.0
# requests in this stack rejects chardet>=6 with dependency warnings
chardet<6

# Evaluation metrics
scipy>=1.10.0
Pillow>=9.0.0
torch-fidelity>=0.3.0

# Optional: Optical flow (temporal metrics)
# opencv-python>=4.8.0

# Optional: FVD (video metrics)
# torchvision already covers InceptionV3 fallback

# Optional: Plotting
# matplotlib>=3.7.0
